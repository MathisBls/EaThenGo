// TODO: React Email template - "Your order is ready!"
// Content: pickup code prominently displayed, establishment name + address

interface OrderReadyEmailProps {
  orderNumber: string;
  pickupCode: string;
  establishmentName: string;
  establishmentAddress: string;
}

export function OrderReadyEmail(props: OrderReadyEmailProps) {
  return null;
}
